<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>CodSoft Landing Page</title>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        /* Header */
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 50px;
            background-color: #1e3a8a;
            color: white;
        }

        header .logo {
            font-size: 24px;
            font-weight: bold;
        }

        header a {
            color: white;
            text-decoration: none;
            margin-left: 20px;
            font-size: 16px;
        }

        /* Hero Section */
        .hero {
            text-align: center;
            padding: 80px 20px;
            background-color: #e0e7ff;
        }

        .hero h1 {
            font-size: 36px;
            margin-bottom: 15px;
        }

        .hero p {
            font-size: 18px;
            margin-bottom: 25px;
            max-width: 700px;
            margin-left: auto;
            margin-right: auto;
        }

        .hero button {
            padding: 12px 30px;
            font-size: 16px;
            background-color: #1e3a8a;
            color: white;
            border: none;
            cursor: pointer;
            border-radius: 5px;
        }

        /* Features Section */
        .features {
            display: flex;
            justify-content: space-around;
            padding: 60px 40px;
            background-color: #f8fafc;
        }

        .box {
            width: 30%;
            padding: 25px;
            background-color: white;
            text-align: center;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }

        .box h3 {
            margin-bottom: 10px;
            color: #1e3a8a;
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 15px;
            background-color: #1e3a8a;
            color: white;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .features {
                flex-direction: column;
                align-items: center;
            }

            .box {
                width: 80%;
                margin-bottom: 20px;
            }

            header {
                flex-direction: column;
            }
        }
    </style>
</head>

<body>

    <!-- Header -->
    <header>
        <div class="logo">CodSoft</div>
        <nav>
            <a href="#">Home</a>
            <a href="#">About</a>
            <a href="#">Services</a>
            <a href="#">Contact</a>
        </nav>
    </header>

    <!-- Hero Section -->
    <section class="hero">
        <h1>Landing Page Project</h1>
        <p>
            A landing page is an ideal web development project for beginners.
            It helps you learn HTML and CSS by creating layouts, sections,
            headers, and footers with clean design.
        </p>
        <button>Get Started</button>
    </section>

    <!-- Features Section -->
    <section class="features">
        <div class="box">
            <h3>Beginner Friendly</h3>
            <p>Easy to understand HTML and CSS concepts.</p>
        </div>

        <div class="box">
            <h3>Creative Design</h3>
            <p>Improves creativity using layouts and colors.</p>
        </div>

        <div class="box">
            <h3>Responsive Layout</h3>
            <p>Works well on mobile and desktop screens.</p>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <p>© 2026 CodSoft. All Rights Reserved.</p>
    </footer>

</body>
</html>
